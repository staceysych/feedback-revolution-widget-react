export enum Progress {
  Discovery = "Discovery",
  Building = "Building",
  Done = "Done",
}

export enum RatingIconType {
  Stars = 'stars',
  Faces = 'faces',
  Thumbs = 'thumbs',
  Hearts = 'hearts'
}
